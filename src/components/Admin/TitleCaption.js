import React from 'react'

export default function TitleCaption(props) {
    return (
        <div className="uppercase font-allerBd font-20 bold center-text margin-top-20">{props.title}</div>
    )
}
